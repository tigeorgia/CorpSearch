# -*- coding: utf-8 -*-
import json
from apps.person.models import Person
import codecs

def load_relations(infile):
    """ Loads a set of Person-Corp relations, adding relations and
    People objects as necessary."""
    pass

def parse_person(person_json):
    """ Takes a json string and converts it into a Person object."""
    pass
